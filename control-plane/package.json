{
  "name": "pixivflow-control-plane",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker clock: cron expression in, authenticated PixivFlow trigger out. Owns no business state.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "@types/node": "^22.20.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.42.0"
  }
}
